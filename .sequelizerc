const dotenv = require('dotenv');

dotenv.config();

module.exports = {
  'models-path': `${__dirname}/src/api/models`,
  'migrations-path':`${__dirname}/migrations-seeds/migrations`,
  'seeders-path':  `${__dirname}/migrations-seeds/seeds`,
  url: process.env.DATABASE_URL,
};
