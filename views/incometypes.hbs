<script src="/javascripts/incometypes.js"></script>
<h1>{{title}}</h1>

<table class="table">
    <tr>
        <th>Тип дохода</th>
        <th>Описание</th>
        <th></th>
    </tr>
    <form id="new" method="post">
        <tr>
            <td><input type="text" name="type" placeholder="Тип"></td>
            <td><input type="text" name="info" placeholder="Описание"></td>
            <td>
                <button formaction="/incometypes" type="submit">Создать</button>
            </td>
        </tr>
    </form>
    {{#each incometypes}}
        <form method="post">
            <tr>
                <td><input id="type_{{this.idIncomeTypes}}" type="text" value="{{this.Type}}" disabled></td>
                <td><input id="info_{{this.idIncomeTypes}}" type="text" value="{{this.Info}}" disabled></td>
                <td>
                    <button id="delete_{{this.idIncomeTypes}}" name="delete" type="button">Удалить</button>
                    <button id="change_{{this.idIncomeTypes}}" name="change" type="button">Редактировать</button>
                </td>
            </tr>
        </form>
    {{/each}}
</table>